import React from "react";

function LoadingPage(props) {
  return (
    <div
      style={{
        display: "flex",
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Loading
    </div>
  );
}

export default LoadingPage;
